# Demucs Backend Configuration

# Port for backend server
PORT=3001

# Demucs binary path (default: 'demucs' assumes it's in PATH)
# If installed via pip: demucs
# If using conda: /path/to/conda/envs/demucs/bin/demucs
DEMUCS_BIN=demucs

# Temporary directory for stems storage
TMP_DIR=/tmp/demucs-stems

# Frontend dev server URL (for CORS reference - currently allows all origins)
FRONTEND_URL=http://localhost:5173

# Max file size in bytes (default: 100MB)
# MAX_FILE_SIZE=104857600

# Request timeout in milliseconds (default: 8 minutes)
# REQUEST_TIMEOUT=480000
