// Re-export schemas from shared package (single source of truth)
export {
  ChordCellSchema,
  IconStepSchema,
  LoopSchema,
  TimelineBlockSchema,
  SongSchema,
  type Loop,
  type Song,
  type ChordCell,
  type IconStep,
  type TimelineBlock,
} from '@music/types/schemas'
